import React from "react";

const Navbar = () => {
  return (
    <nav className="sticky top-0 z-20 mx-auto w-full bg-bg-primary p-5">
      Navbar
      <span className="absolute  bottom-0 left-0 h-[1px] w-full bg-gradient-to-br from-blue-secondary to-blue-primary"></span>
    </nav>
  );
};

export default Navbar;
