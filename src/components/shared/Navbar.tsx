import React from "react";

const Navbar = () => {
  return (
    <nav className="sticky top-0 z-50 mx-auto w-full bg-bg-primary p-5 px-[min(10vh,10vh)] shadow-primary max-lg:px-[min(6vh,6vh)] max-sm:px-[min(2vh,2vh)]">
      krishna
    </nav>
  );
};

export default Navbar;
